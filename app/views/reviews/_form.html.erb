<%= form_for([@movie, @review]) do |f| %>
<%= f.label :movie_id %>
<%= f.collection_select(:movie_id, Movie.all, :id, :name, {include_blank: true}) %>
<br>
<%= f.label :title %>
<%= f.text_field :title %>
<br>
<%= f.label :body %>
<%= f.text_select :body %>
<br>
<%= f.submit %>
<% end %>
