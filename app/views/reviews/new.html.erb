<h1>New Review</h1>
<%= form_for [@movie, @review] do |f| %>
<%# f.label :movie_id %>
<%# f.collection_select :movie_id, Movie.all, :id, :name, {include_blank: "All Movies"} %><br>
<%= f.label :title %>
<%= f.text_field :title %><br>
<%= f.label :body %>
<%= f.text_area :body %><br>
<%= f.submit "Submit" %>
<% end %>
