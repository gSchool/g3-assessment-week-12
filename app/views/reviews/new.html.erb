<h1>New Review</h1>

<%= form_for [@movie, @review] do |f| %>

<% if @review.errors.any? %>
  <div>
    <h2><%= pluralize(@review.errors.count, "error") %> prohibited this <%= @review.class %> from being saved:</h2>

    <ul>
    <% @review.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<% unless @movie.id == nil %>
  <%= f.label :movie_id %><br>
  <%= f.collection_select :movie_id, Movie.all, :id, :name, {include_blank: 'All Movies'} %><br><br>
<% end %>

  <%= f.label :title %><br>
  <%= f.text_field :title %><br><br>

  <%= f.label :body %><br>
  <%= f.text_field :body %><br><br>

  <%= f.submit %>

<% end %>
