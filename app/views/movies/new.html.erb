<%= form_for(@movie, :html => {class: "form-horizontal"}) do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :year %>
<%= f.number_field :year %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.submit %>

<% end %>
