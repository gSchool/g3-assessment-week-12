<h1>Cageflix</h1>
<h3>Netflix for Nic Cage Movies</h3>
<%= link_to "New Movie", new_movie_path %>

<% @movies.each do |movie| %>
  <p><b><%= movie.name %> (<%= movie.year.strftime("%Y") %>)</b> <%= link_to "New Review", new_movie_review_path(movie) %></p>
  <p> <%= movie.synopsis %></p>
<% end %>
